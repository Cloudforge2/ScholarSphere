spring:
  
  neo4j:
    uri: bolt://localhost:7687
    authentication:
      username: neo4j
      password: ${NEO4J_PASSWORD}
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
# logging:
#   level:
#     org:
#       hibernate:
#         type:
#           descriptor:
#             sql: trace
#         stat: debug

server:
  port: 8080