requests>=2.28.2
aiohttp>=3.8.4
PyPDF2>=3.0.0
pdfplumber>=0.7.6
# Optional (useful for faster XML/HTML handling or improved PDF extraction)
# lxml>=4.9.2
# pdfminer.six>=20221105

# Note: The script uses only standard-library modules for most tasks.
# The packages above are required for HTTP requests, async fetching, and
# PDF extraction. Install with:
#    pip install -r requirements.txt
Allow
Skip
